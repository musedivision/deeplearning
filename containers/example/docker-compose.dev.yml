  CONTAINER_REL_PATH:
    container_name: CONTAINER_NAME
    build: containers/CONTAINER_REL_PATH/.
    volumes:
      - ./containers/CONTAINER_REL_PATH/src:/var/www/app/src
    environment:
      - DATABASE_URL=postgres://postgres:password@postgres:5432/propertymgr_daily_snapshot
    ports:
        - 8888:8888